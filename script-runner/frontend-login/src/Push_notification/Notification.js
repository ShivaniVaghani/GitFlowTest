// frontend-login/src/Push_notification/Notification.js

// A no-op stub for push tokens in the login-only flow
export const generateToken = async () => {
  // we don’t need a real token for just logging in,
  // so return an empty string (or any placeholder)
  return "";
};
